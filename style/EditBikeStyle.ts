import { StyleSheet } from "react-native";

export const EditBikeStyle = StyleSheet.create({
  scrollView: {},
  container: {
    alignItems: "center",
    marginTop: 10,
    justifyContent: "center",
  },
  card: {
    borderRadius: 20,
    alignItems: "center",
    justifyContent: "center",
  },
  wellcomeText: {
    lineHeight: 30,
    fontSize: 26,
    marginTop: 2,
  },
  editField: {
    marginTop: 30,
  },
  inputFields: {
    alignItems: "center",
    justifyContent: "space-between",
    margin: 4,
  },
});
