import { StyleSheet } from "react-native";

export const registerText = StyleSheet.create({
  firstNamePosition: {
    paddingRight: 2,
    margin: 4,
    marginTop: 8,
    marginBottom: -1,
  },
});
