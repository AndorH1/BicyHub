import { StyleSheet } from "react-native";

export const TimeLineStyle = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
    paddingTop: 5,
  },
  list: {
    flex: 1,
    marginTop: 20,
  },
});
